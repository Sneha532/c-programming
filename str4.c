#include<stdio.h>
#include<string.h>
int main()
{
    char A[20],B[20];
    int n,m,val;
    scanf("%s%s",A,B);
    n=strlen(A);
    m=strlen(B);
    printf("the length of the string are %d and %d",n,m);
    printf("%d",val);

}